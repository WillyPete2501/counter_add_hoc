
const BackButton = ({ resetCount }) => {
    return (
        <div>
            <h1>Completed</h1>
            <button onClick={resetCount}>Back Button</button>
        </div>
    )

}

export default BackButton